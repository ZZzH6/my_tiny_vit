# AI Coding Agent Instructions

## Project Overview
This is a minimal Python project consisting of a single script that demonstrates basic output functionality.

## Key Files
- `hello_word.py`: Main script (note: filename contains intentional typo "word" instead of "world")

## Architecture
- Standalone Python 3 script with no external dependencies
- Single entry point that executes a simple print statement

## Developer Workflows
- **Running the script**: Execute `python3 ./hello_word.py` from the project root
- No build process required
- No testing framework in place

## Code Patterns
- Use `print()` for console output
- Simple, linear execution flow
- No error handling or input processing

## Conventions
- Filenames may include intentional variations (e.g., "word" vs "world")
- Minimal code structure with direct execution

## Dependencies
- None required (uses only Python standard library)

## Integration Points
- None (isolated script)